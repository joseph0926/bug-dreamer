{
  "name": "bug-dreamer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "packageManager": "pnpm@11.17.0",
  "dependencies": {
    "canonicalize": "4.0.0"
  },
  "scripts": {
    "runner:prepare": "node scripts/prepare-image.mjs",
    "runner:run": "node scripts/run-scenario.mjs",
    "runner:batch": "node scripts/run-batch.mjs",
    "runner:digest": "node scripts/run-digest.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
