# Nightmares — 2026-08-31

Target: https://github.com/joseph0926/firsttx @ f624b09f148c3368a51807f48d3237db20cef9c6 — packages/tx
Run: manual, isolated

## 1. The failed step never cleans up after itself

### Incident timeline (fiction, future-dated)

- 2026-11-14 09:12 — A checkout flow wraps inventory reservation and payment capture in one transaction. Payment capture reserves a hold at the provider, then throws on a declined card.
- 2026-11-14 09:12 — The transaction reports a successful rollback. Inventory is released. The payment hold is not.
- 2026-11-21 10:40 — Finance reconciliation finds a week of orphaned payment holds. Every one traces back to a step that started, produced a side effect, and failed before completing.

### Reproduction (fact)

```bash
node scripts/run-scenario.mjs --scenario 'scenarios/firsttx/failed-step-compensation.test.ts' --timeout-ms 30000
```

Observed: failed 4/4 runs (3 consecutive recorded). Evidence: `evidence/2026-08-31/failed-step-compensation.run-{1,2,3}.json`

Third-party reproduction: an independent agent session on the same host ran the recorded command unchanged against the same image and observed the same failure signature once (2026-08-31, candidate-failure, exit 1). Accepted by the user as satisfying the reproduction rule; a different-machine reproduction has not been performed.

### Failing test (fact)

- Test: `scenarios/firsttx/failed-step-compensation.test.ts` (sha256:e59c7f4e14452d18efd088ca2207e26b945ca6fbffc8072b3177d59368682495)
- Expectation basis: documentation — `packages/tx/README.md:148` states the failed step's own compensation runs first: "1. Execute undo3 (if step3 started)"
- Control: a failed step without compensation triggers reverse-order rollback of completed steps (`packages/tx/tests/transaction.test.ts:614-654` pattern) — passed
- Expected: `["step1-run", "step2-start", "step2-compensate", "step1-compensate"]`
- Actual: `["step1-run", "step2-start", "step1-compensate"]`
- Failure signature: oracle violation — the failed step's registered `compensate` is never invoked; only completed steps are compensated

### Relevant code path (fact)

- `packages/tx/src/transaction.ts:102` — `completedSteps` increments only after a step resolves successfully
- `packages/tx/src/transaction.ts:120` — the catch path calls `rollback(stepIndex, error)` with the failed step's index
- `packages/tx/src/transaction.ts:218` — the rollback loop starts at `completedSteps - 1`, so the failed step at `stepIndex` is excluded even when it registered a `compensate`

### Impact note (estimate)

Any consumer that registers `compensate` on a step whose side effects precede its failure point is reachable: the step starts, mutates external state, throws, and its own cleanup silently never runs while the transaction reports rollback success. The README is internally inconsistent — line 138 says "all completed steps are rolled back", while the example at line 148 promises "Execute undo3 (if step3 started)". The implementation follows line 138, so either the rollback loop must include the failed step or the documented example is wrong. Until one side is fixed, users who trusted the example leak exactly one uncompensated side effect per failed transaction.

### Human verdict

- [v] Real bug, worth fixing
- [ ] Real bug, not worth fixing
- [ ] Expectation was wrong
- [ ] Undecided

Notes:

## 2. The upgrade that seeds poison and then burns the field

Module: packages/local-first (incident-seeded from PR #129)

### Incident timeline (fiction, future-dated)

- 2027-01-05 07:30 — A release bumps a model's `version` to 3 and ships a new default settings object. A refinement in the zod schema now requires a field the default object predates.
- 2027-01-05 07:31 — On first load after the upgrade, every client deletes its valid version-2 user settings and seeds the schema-invalid default in their place. Screens render from the invalid object.
- 2027-01-05 07:32 — On the next load, validation rejects the seeded row and deletes it too. Users end the morning with no settings at all, and support cannot tell which of the two deletions lost the data.

### Reproduction (fact)

```bash
node scripts/run-scenario.mjs --scenario 'scenarios/firsttx/incident-seeded-2026-08-31/local-first/incident-03.test.ts' --module packages/local-first --timeout-ms 30000
```

Observed: failed 4/4 runs (3 consecutive recorded). Evidence: `evidence/2026-08-31/incident-seeded/batch-lf.json`

Third-party reproduction: an independent codex agent session on the same host ran the recorded command unchanged and observed the same failure signature once (2026-08-31, candidate-failure). Evidence: `evidence/2026-08-31/incident-seeded/independent-repro-codex.json`

### Failing test (fact)

- Test: `scenarios/firsttx/incident-seeded-2026-08-31/local-first/incident-03.test.ts`
- Expectation basis: documentation — `packages/local-first/README.md` "Schema Validation": "Zod schema protects against corrupted IndexedDB data" and "Invalid data is rejected" (ValidationError example). Corroborating code paths: `storage-manager.ts:66-85` deletes stored data that fails `schema.safeParse`, and `sync-manager.ts:144-157, 206-219` validate before every save. Citation corrected 2026-08-31 after external review; the documentation predates the scenario
- Expected: loading after a version bump either seeds a schema-valid value or refuses the seed; any value `load()` returns satisfies the schema
- Actual: `{ loadOutcome: "returned", returnedSatisfiesSchema: false, persistedSatisfiesSchema: false }` (schema `z.number().max(10)`, seeded `initialData` 99)
- Failure signature: oracle violation — `expected false to be true`; the version-reset branch persists and returns unvalidated `initialData`

### Relevant code path (fact)

- `packages/local-first/src/storage-manager.ts:46-62` — on `_v` mismatch the stored row is deleted and `initialData` is saved and returned with no `safeParse`
- `packages/local-first/src/storage-manager.ts:66-85` — the normal path validates and destroys anything invalid, so the seeded row is deleted on the following load
- `packages/local-first/src/sync-manager.ts:206-219` — even `patch()` drafts built from the same `initialData` are validated before save

### Impact note (estimate)

Reachable on any version bump where `initialData` violates a schema refinement — `initialData` is only type-checked (`T`), so `0`, `''`, or an outdated default object compile fine while failing `min`, `max`, `email`, or `refine` rules. One upgrade destroys valid user data twice: first replaced by an invalid seed, then the seed itself is deleted as corrupt. This is the one write path in the package that skips validation, and it is the same branch PR #137 widened by making falsy seeds count as provided.

### Human verdict

- [v] Real bug, worth fixing
- [ ] Real bug, not worth fixing
- [ ] Expectation was wrong
- [ ] Undecided

Notes: verdict recorded 2026-08-31 by the user over the incident-seeded candidate set.

## 3. The receipt that gets refunded anyway

Module: packages/tx (incident-seeded from PR #129)

### Incident timeline (fiction, future-dated)

- 2027-02-10 14:03 — A checkout handler starts a transaction: reserve inventory, then capture payment with a retry policy. A double-clicked confirm button triggers a code path that calls `tx.commit()` while the capture step is still awaiting the provider.
- 2027-02-10 14:03 — `commit()` resolves. The UI shows the order as confirmed and navigates away.
- 2027-02-10 14:04 — The in-flight capture exhausts its retries and fails. The transaction, already committed, rolls back anyway: the inventory reservation is compensated while the confirmation screen the user saw stands. The order exists for the user and for support, but not in stock.

### Reproduction (fact)

```bash
node scripts/run-scenario.mjs --scenario 'scenarios/firsttx/incident-seeded-2026-08-31/tx/incident-01.test.ts' --module packages/tx --timeout-ms 30000
```

Observed: failed 4/4 runs (3 consecutive recorded). Evidence: `evidence/2026-08-31/incident-seeded/batch-tx.json`

Third-party reproduction: an independent codex agent session on the same host ran the recorded command unchanged and observed the same failure signature once (2026-08-31, candidate-failure). Evidence: `evidence/2026-08-31/incident-seeded/independent-repro-codex.json`

### Failing test (fact)

- Test: `scenarios/firsttx/incident-seeded-2026-08-31/tx/incident-01.test.ts`
- Expectation basis: documentation — `packages/tx/README.md` "After commit": the transaction becomes immutable; `packages/tx/src/types.ts` `TxStatus` lifecycle where `committed` is terminal
- Expected: either `commit()` during a running step throws `TransactionStateError`, or once `commit()` resolves no compensation runs and the state stays `committed` (the oracle accepts both)
- Actual: `{ commitOutcome: "resolved", stepOutcome: "Retry exhausted for step-1", compensated: ["step-1"], stateAfter: "rolled-back" }`
- Failure signature: oracle violation — `expected [ 'step-1' ] to deeply equal []`; compensations execute after a successful commit and the state leaves `committed`

### Relevant code path (fact)

- `packages/tx/src/transaction.ts:138-143` — `commit()` checks `status` but not `isStepRunning`, so it accepts a commit mid-step and marks the transaction committed
- `packages/tx/src/transaction.ts:112-121` — the still-running step's failure unconditionally calls `rollback(stepIndex, error)`
- `packages/tx/src/transaction.ts:212` — `rollback` overwrites `committed` with `rolled-back` and compensates completed steps

### Impact note (estimate)

Requires calling `commit()` without awaiting the in-flight step, so it needs a caller-side race (double submission, parallel handlers) — less reachable than #2 and #3, but the broken promise is the library's strongest one: a resolved `commit()` is not final. Whichever fix direction is chosen (reject commit while a step runs, or make commit win), the current behavior violates the documented lifecycle either way.

### Human verdict

- [v] Real bug, worth fixing
- [ ] Real bug, not worth fixing
- [ ] Expectation was wrong
- [ ] Undecided

Notes: verdict recorded 2026-08-31 by the user over the incident-seeded candidate set.

## 4. The error that loses its name in transit

Module: packages/tx (phase 4 re-measurement, control arm)

### Incident timeline (fiction, future-dated)

- 2027-03-03 11:20 — A checkout step throws `PaymentDeclinedError`. The caller's `catch` distinguishes declined cards (show a message) from network faults (offer retry).
- 2027-03-03 11:20 — What actually arrives is `RetryExhaustedError: Retry exhausted for step-0`, even though the step declared no retry config. The type-based branch falls through to the generic failure page.
- 2027-03-10 16:05 — Support tickets show every distinct failure cause rendering as the same generic error. The original errors exist only inside a wrapper field nobody reads.

### Reproduction (fact)

```bash
node scripts/run-scenario.mjs --scenario 'benchmark/rerun-2026-08-31/control/tx/control-02.test.ts' --module packages/tx --timeout-ms 30000
```

Observed: failed 4/4 runs (3 consecutive recorded). Evidence: `evidence/2026-08-31/rerun/control/clean-tx.json`

Third-party reproduction: an independent codex agent session on the same host ran the recorded command unchanged and observed the same failure signature once (2026-08-31, candidate-failure). Evidence: `evidence/2026-08-31/rerun/independent-repro-codex.json`

The same mechanism was independently generated by three separate rerun sessions (baseline-01, control-02, separated-01 and -03), each failing with a wrapped error where the original was documented.

### Failing test (fact)

- Test: `benchmark/rerun-2026-08-31/control/tx/control-02.test.ts`
- Expectation basis: documentation — `packages/tx/README.md` "Automatic Rollback" list item 4: "Re-throw original error" (the example steps declare no retry config)
- Expected: the caller catches the original `TypeError` with message `original-boom`, not a wrapper error type
- Actual: `{ name: "RetryExhaustedError", message: "Retry exhausted for step-0" }`
- Failure signature: oracle violation — `expected 'RetryExhaustedError' to be 'TypeError'`

### Relevant code path (fact)

- `packages/tx/src/retry.ts:15-18` — a step without retry config gets `DEFAULT_RETRY_CONFIG` (`maxAttempts: 1`), so every plain step runs through the retry machinery
- `packages/tx/src/retry.ts:49-50` — on the final attempt the original error is replaced by `new RetryExhaustedError(stepId, maxAttempts, errors)`

### Impact note (estimate)

Every failing step without retry config is affected — the most common configuration. Error-type-based handling (`instanceof PaymentDeclinedError`, HTTP status mapping, retry-vs-fatal branching) silently stops working because callers receive `RetryExhaustedError` regardless of cause; the documented contract promises the original error. The original is recoverable from the wrapper's `errors` array, but no README example reads it.

### Human verdict

- [v] Real bug, worth fixing
- [ ] Real bug, not worth fixing
- [ ] Expectation was wrong
- [ ] Undecided

Notes: verdict recorded 2026-08-31 by the user over the rerun verdict sheet (rows 1, 5, 12, 13).

## 5. The screen that stops listening to history

Module: packages/local-first (phase 4 re-measurement, control arm)

### Incident timeline (fiction, future-dated)

- 2027-04-07 09:00 — A dashboard shows "last updated 3 minutes ago" from the model's history. The app refreshes history metadata after a background load.
- 2027-04-07 09:01 — The store's combined snapshot is replaced with the new history, but no subscriber is notified. React, wired through `useSyncExternalStore`, never re-renders.
- 2027-04-07 17:30 — Users report the staleness badge freezing at whatever it showed on mount, while the underlying data quietly moved on.

### Reproduction (fact)

```bash
node scripts/run-scenario.mjs --scenario 'benchmark/rerun-2026-08-31/control/local-first/control-06.test.ts' --module packages/local-first --timeout-ms 30000
```

Observed: failed 4/4 runs (3 consecutive recorded). Evidence: `evidence/2026-08-31/rerun/control/clean-local-first.json`

Third-party reproduction: an independent codex agent session on the same host ran the recorded command unchanged and observed the same failure signature once (2026-08-31, candidate-failure). Evidence: `evidence/2026-08-31/rerun/independent-repro-codex.json`

The same mechanism was independently generated by the baseline session (baseline-04, `expected +0 to be 1`).

### Failing test (fact)

- Test: `benchmark/rerun-2026-08-31/control/local-first/control-06.test.ts`
- Expectation basis: documentation — `packages/local-first/README.md` "React integration via `useSyncExternalStore`": a store must notify its subscribers whenever the snapshot it serves changes, and `CacheManager.subscribe` is that store subscription
- Expected: the combined snapshot is replaced after `setHistory` and the subscriber is notified of that change
- Actual: `{ stableBefore: true, snapshotReplaced: true, historyUpdatedAt: 123, notified: 0 }`
- Failure signature: oracle violation — `expected 0 to be greater than or equal to 1`; the snapshot changed with zero notifications

### Relevant code path (fact)

- `packages/local-first/src/cache-manager.ts:89-92` — `setHistory` assigns the history and calls `updateSnapshot()` but never `notifySubscribers()`
- `packages/local-first/src/cache-manager.ts:60-65` — `updateWithData` shows the intended pattern: snapshot update followed by notification
- `packages/local-first/src/sync-manager.ts:66-67` — the initial-load path calls `setHistory` after `updateWithData`, so the history part of that update is exactly the silent one

### Impact note (estimate)

Any UI that renders history metadata (staleness badges, "last updated" labels, conflict indicators) misses every update that arrives through `setHistory` alone and re-renders only when some later data change happens to notify. The failure is invisible in tests that read the store directly and only appears through the React subscription contract.

### Human verdict

- [v] Real bug, worth fixing
- [ ] Real bug, not worth fixing
- [ ] Expectation was wrong
- [ ] Undecided

Notes: verdict recorded 2026-08-31 by the user over the rerun verdict sheet (rows 4, 11).

## 6. The megaphone that explodes after you hang it up

Module: packages/local-first (phase 4 re-measurement, control arm)

### Incident timeline (fiction, future-dated)

- 2027-05-12 14:45 — An app tears down its data layer on logout and calls the broadcaster's `close()`. Moments later a background flush finishes and calls `model.replace()` one last time.
- 2027-05-12 14:45 — `replace()` succeeds in storage, then the post-write broadcast throws `InvalidStateError` from the closed channel. The logout flow dies on an unhandled exception after the data was already saved.
- 2027-05-19 10:10 — The crash reporter shows a spike of `InvalidStateError` at logout and hot-reload, none reproducible in tests because tests never reuse a closed singleton.

### Reproduction (fact)

```bash
node scripts/run-scenario.mjs --scenario 'benchmark/rerun-2026-08-31/control/local-first/control-03.test.ts' --module packages/local-first --timeout-ms 30000
```

Observed: failed 4/4 runs (3 consecutive recorded). Evidence: `evidence/2026-08-31/rerun/control/clean-local-first.json`

Third-party reproduction: an independent codex agent session on the same host ran the recorded command unchanged and observed the same failure signature once (2026-08-31, candidate-failure). Evidence: `evidence/2026-08-31/rerun/independent-repro-codex.json`

The same mechanism was independently generated by the baseline session (baseline-03).

### Failing test (fact)

- Test: `benchmark/rerun-2026-08-31/control/local-first/control-03.test.ts`
- Expectation basis: documentation — `packages/local-first/src/broadcast.ts` FallbackChannel docstring: "Provides no-op methods to allow graceful degradation without crashes"; `packages/local-first/README.md`: without BroadcastChannel, cross-tab propagation is skipped
- Expected: a broadcast issued after `close()` is skipped as unavailable cross-tab propagation and does not throw
- Actual: `"InvalidStateError"`
- Failure signature: oracle violation — `expected 'InvalidStateError' to be 'no-throw'`

### Relevant code path (fact)

- `packages/local-first/src/broadcast.ts:160-163` — `close()` closes the underlying channel but leaves the singleton and its `channel` reference in place
- `packages/local-first/src/broadcast.ts:90-95` — `getInstance()` keeps returning the closed instance; nothing resets it
- `packages/local-first/src/broadcast.ts:117-124` — `broadcast()` calls `postMessage` unconditionally; on a closed `BroadcastChannel` that throws `InvalidStateError`

### Impact note (estimate)

Reachable in teardown ordering races: logout, page transition, test cleanup, or hot module reload closing the broadcaster while a late write is still in flight. The declared design point is graceful degradation when cross-tab messaging is unavailable, and a closed channel is precisely that state — instead the write path that already persisted data throws on its final, optional step.

### Human verdict

- [v] Real bug, worth fixing
- [ ] Real bug, not worth fixing
- [ ] Expectation was wrong
- [ ] Undecided

Notes: verdict recorded 2026-08-31 by the user over the rerun verdict sheet (rows 3, 9).

## 7. Zero tries, one anonymous explosion

Module: packages/tx (phase 4 re-measurement, control arm)

### Incident timeline (fiction, future-dated)

- 2027-06-02 13:00 — A feature flag computes retry budgets dynamically and, during an incident, sets `maxAttempts: 0` to shed load.
- 2027-06-02 13:00 — Steps now fail with a bare `Error: [FirstTx] Unexpected: retry loop ended without return or throw` — an internal invariant message — instead of the documented `RetryExhaustedError`. Alerting keyed on error types misses all of them.

### Reproduction (fact)

```bash
node scripts/run-scenario.mjs --scenario 'benchmark/rerun-2026-08-31/control/tx/control-04.test.ts' --module packages/tx --timeout-ms 30000
```

Observed: failed 4/4 runs (3 consecutive recorded). Evidence: `evidence/2026-08-31/rerun/control/clean-tx.json`

Third-party reproduction: an independent codex agent session on the same host ran the recorded command unchanged and observed the same failure signature once (2026-08-31, candidate-failure). Evidence: `evidence/2026-08-31/rerun/independent-repro-codex.json`

### Failing test (fact)

- Test: `benchmark/rerun-2026-08-31/control/tx/control-04.test.ts`
- Expectation basis: documentation — `packages/tx/README.md` "tx.run" Throws: "RetryExhaustedError - When all retry attempts fail"; `packages/tx/src/types.ts` `RetryConfig.maxAttempts` doc
- Expected: with zero permitted attempts the step function is never invoked and the failure is reported through the declared `RetryExhaustedError` channel
- Actual: `{ outcome: "Error", calls: 0 }`
- Failure signature: oracle violation — `expected 'Error' to be 'RetryExhaustedError'`

### Relevant code path (fact)

- `packages/tx/src/retry.ts:37` — the attempt loop `for (attempt = 1; attempt <= maxAttempts; ...)` never runs when `maxAttempts` is 0
- `packages/tx/src/retry.ts:69` — control falls through to `throw new Error('[FirstTx] Unexpected: retry loop ended without return or throw')`, leaking the internal invariant error to callers

### Impact note (estimate)

Requires a caller to pass `maxAttempts: 0`, which the type permits but no documented example uses; the blast radius is an undocumented error type rather than data loss. Judged real but low priority: either clamp or reject zero at the boundary, or emit the documented error.

### Human verdict

- [ ] Real bug, worth fixing
- [v] Real bug, not worth fixing
- [ ] Expectation was wrong
- [ ] Undecided

Notes: verdict recorded 2026-08-31 by the user over the rerun verdict sheet (row 8, worth-fixing split delegated).
